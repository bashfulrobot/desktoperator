# Direnv configuration
# Managed by Ansible - direnv role

# Set direnv fish mode (eval_on_arrow is default)
# Options: eval_on_arrow, eval_after_arrow, disable_arrow
set -g direnv_fish_mode eval_on_arrow

# Initialize direnv hook
if type -q direnv
    direnv hook fish | source
end
