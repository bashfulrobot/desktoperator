<network>
  <name>{{ item.name }}</name>
  <forward mode='route'/>
  <bridge name='{{ item.bridge }}' stp='on' delay='0'/>
  <ip address='{{ item.gateway }}' netmask='255.255.255.0'>
    <dhcp>
      <range start='{{ item.dhcp_start }}' end='{{ item.dhcp_end }}'/>
    </dhcp>
  </ip>
</network>
