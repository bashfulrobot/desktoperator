# ncspot configuration for native installation
# Managed by Ansible - spotify role

# ncspot terminal Spotify client (GitHub release)
# Binary installed to {{ ncspot_install_dir }}/ncspot
# Config located at ~/.config/ncspot/config.toml
