---
# Derived data for Codex CLI MCP configuration. The server list lives in
# files/mcp-sites.json so both the Ansible role and helper scripts can share a
# single source of truth.

codex_cli_mcp_sites: "{{ lookup('ansible.builtin.file', role_path + '/files/mcp-sites.json') | from_json }}"

