matches:
  - trigger: ":ctm"
    replace: "{{mydate}} T-Mobile/Sysdig - Cadence Call"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ":clu"
    replace: "{{mydate}} Lululemon/Sysdig - Cadence Call"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ":cal"
    replace: "{{mydate}} Alaska Airlines/Sysdig - Cadence Call"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ":cgb"
    replace: "{{mydate}} Gov BC/Sysdig - Cadence Call"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ":cmb"
    replace: "{{mydate}} Modusbox/Sysdig - Cadence Call"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ":cnc"
    replace: "{{mydate}} NC Interactive/Sysdig - Cadence Call"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ":csc"
    replace: "{{mydate}} Suncor/Sysdig - Cadence Call"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"