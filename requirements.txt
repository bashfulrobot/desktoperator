# Python requirements for Desktop Operator
# NOTE: On Ubuntu/Pop!_OS, packages are installed via system package managers
# This file is for reference and non-Ubuntu systems only

# Ansible version reference (installed via apt from ppa:ansible/ansible)
# ansible==12.1.0

# Ansible linting (installed via pipx on Ubuntu/Pop!_OS)
# ansible-lint is installed via pipx to ensure compatibility with newer ansible-core
# ansible-lint==25.9.2

# Additional useful tools (if needed)
# jmespath==1.0.1
# netaddr==1.3.0
